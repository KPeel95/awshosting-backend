{
  "name": "aws-ebs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-svelte": "cd ../svelte-app && npm install",
    "build": "cd ../svelte-app && vite build",
    "dev:frontend": "cd ../svelte-app && vite",
    "dev:backend": "node index.js",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"", // Runs front and back on separate ports, for development
    "start": "node index.js" // Runs the server, remember to build the front end first, or it will not work, FOR PRODUCTION
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^6.2.2",
    "nodemon": "^3.1.4",
    "terser": "^5.31.6",
    "vite": "^6.2.2"
  }
}
